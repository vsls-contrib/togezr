/**
 * POST /api/branch-broadcast
 * Create branch broadcast record.
 */
export const createBranchBroadcast = (req: Request, res: Response) => {
    // 0. validate user identity
    // 1. get the broadcast data
    // 2. save the data
    // 3. return OK
};

/**
 * GET /api/branch-broadcasts
 * List current branch broadcasts.
 */
export const getBranchBroadcasts = (req: Request, res: Response) => {
    // 0. validate user identity
    // 1. get list of user branch broadcasts
    // 3. return the list
};

